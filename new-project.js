function project(nameArchitect, projectCount) {
    let hours = projectCount * 3;
console.log(`The architect ${nameArchitect} will need ${hours} hours to complete ${projectCount} project/s.`);
}
project("Georgi", 9);